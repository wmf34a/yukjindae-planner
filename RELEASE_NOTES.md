# 릴리즈 노트 — 육진대 × 원주여행 (v1.0)

**배포일**: 2026-08-21
**배포 URL**: https://yukjindae-planner.pages.dev
**저장소**: https://github.com/wmf34a/yukjindae-planner
**형태**: 정적 단일 페이지(`index.html` 1개, 빌드 도구/프레임워크 없음)

## 한 줄 요약

육진대 멤버들이 실제로 다녀온 원주 하루 여행(소금산 그랜드밸리 → 미디어아트센터 →
온정리닭갈비 → 국립강원전문과학관 → 스톤크릭 → 오크밸리리조트)을 소개하는 정적
웹페이지. 육진대 맵 앱과 톤·컬러·로고를 맞춰서, 향후 "육진대 여행플래너"로 확장할
때의 프로토타입 성격도 겸한다.

---

## 주요 기능

- **코스목록 탭**: 6곳을 실제 방문 순서 카드로 나열, 카드 사이에 OSRM 실도로 기준
  자차/도보 이동 정보 표시, 상단 요약 배너(총 장소 수·이동시간·거리)
- **지도 탭**(기본 화면): 네이버 지도 오버뷰 + 번호 핀·이름 라벨, 이전/다음 버튼으로
  구간 이동 시 차량 아이콘이 실도로 경로를 따라 애니메이션(카메라 추적 포함),
  핀을 직접 클릭하면 그 장소로 즉시 이동 + 요약 팝업, 내 위치 버튼(GPS)
- **상세보기 모달**: 슬라이드업, 실제 사진(가능한 곳)/카테고리 일러스트, 운영시간,
  입장료 또는 메뉴 링크, 전화 연결, 네이버 지도 미니맵 + 길찾기 버튼
- **SNS 섹션**: 코스목록 하단에 육진대 SNS 7종(인스타/유튜브/틱톡/네이버클립/
  네이버카페/카카오톡 공지방·수다방) — 육진대맵과 동일한 브랜드 아이콘 사용
- **디자인**: 육진대맵과 동일한 네이비/블루 팔레트, 헤더에 곰돌이 캐릭터 로고
  (육진대맵 저장소의 `character-logo.svg`를 인라인 SVG로 내장)

## 데이터 출처

| 장소 | 주소/좌표/운영시간/요금 | 사진 |
|---|---|---|
| 소금산 그랜드밸리 | 육진대 맵 장소 DB(Notion) 대조 | 육진대 맵 DB (실사) |
| 소금산미디어아트센터 | 육진대 맵 장소 DB(Notion) 대조 | 육진대 맵 DB (실사) |
| 스톤크릭 | 육진대 맵 장소 DB(Notion) 대조 | 육진대 맵 DB (실사) |
| 국립강원전문과학관 | 육진대 맵 장소 DB(Notion) 대조 | 육진대 맵 DB (실사) |
| 온정리닭갈비금강막국수 원주점 | 사용자 직접 확인(운영시간/전화번호/메뉴 링크) | 없음 — 공식 소스가 없어 미사용 |
| 오크밸리리조트 힐스빌리지 | 웹 조사 + 사용자 확인 | 오크밸리 공식 홈페이지(oakvalley.co.kr) |

좌표는 육진대 맵 DB에 있는 4곳을 제외하면 무료 지오코딩(OpenStreetMap Nominatim)
기반 도로 단위 근사치다.

## 기술 스택 / 설계 메모

- 지도: 네이버 지도 JS API v3 (`CONFIG.naverMapClientId`)
- 경로: OSRM 무료 라우팅(`router.project-osrm.org`) — 실도로 거리/시간, 실패 시
  직선거리 추정으로 폴백
- 배포: Cloudflare Pages(`wrangler pages deploy`), GitHub `main` 브랜치에 push
- 시크릿: `.dev.vars`(Naver Client Secret 등)는 `.gitignore` 처리, 이 페이지는
  Dynamic Map JS SDK만 쓰므로 Client Secret 자체가 불필요

## 이번 세션에서 고친 주요 버그

- 네이버 지도 SDK `callback`이 `window.naver` 할당 완료 전에 불려 `ReferenceError`
  나던 타이밍 버그 → `setTimeout`으로 한 틱 지연
- `.map-fallback-full`에 `display:flex`가 `[hidden]` 기본 규칙보다 우선 적용되어
  지도가 정상 로드돼도 폴백 화면이 안 사라지던 CSS 명시도 버그
- 이동 애니메이션이 `requestAnimationFrame`에만 의존해 탭이 백그라운드로 가면
  완료 콜백이 영영 안 불려 버튼이 멈추던 문제 → `setTimeout` 안전장치 추가,
  애니메이션 시간도 2.6초 → 1.1초로 단축
- 지도 탭 진입 시 하단 이전/다음 패널이 화면 밖으로 밀리던 레이아웃 문제 →
  지도 탭만 JS로 실측 높이에 맞추는 방식으로 수정(코스목록은 원래의 자연스러운
  페이지 스크롤 유지)
- 핀 클릭 시 정보 팝업만 뜨고 "현재 위치"가 갱신되지 않아, 클릭한 장소가 아니라
  이전 순번 기준으로 다음/이전이 동작하던 문제 → 핀 클릭 시 그 장소로 즉시 이동
  (`jumpToStop`)하도록 수정

## 알려진 제한사항 / 다음에 할 일

- **온정리닭갈비 사진 없음**: 공식 홈페이지가 없고 출처 불명 사진을 무단으로 쓰는
  건 피했다. 육진대 맵 장소 DB에 실사를 올려두면 다음에 그대로 반영 가능.
- **좌표 정밀도**: 온정리닭갈비·오크밸리는 무료 지오코딩 근사치라 정확한 핀 위치가
  약간 다를 수 있다. 육진대 맵 장소 DB에 두 곳을 등록해두면 앱과 데이터를 공유할
  수 있다.
- **Client ID 노출**: `CONFIG.naverMapClientId`는 브라우저에 노출되는 공개 키라
  문제없지만, 네이버 클라우드 콘솔의 Web 서비스 URL 허용 목록에 배포 도메인이
  등록되어 있어야 지도가 뜬다(이미 등록·확인됨).

## 다음 단계: 육진대 여행플래너로 확장할 때 참고

이 페이지는 원주 여행 하나만을 위한 정적 프로토타입이지만, 코드 안에는 향후
플래너 앱에 재사용할 수 있는 로직을 의도적으로 남겨뒀다:

- `ROUTE_ORDER`는 지금은 "실제로 다녀온 순서"를 그대로 쓰지만, 지리적 최적 동선
  계산 로직에 대한 메모가 코드 주석으로 남아있다(스톤크릭↔온정리 순서 교체 시
  약 1.6km 단축 가능했던 사례).
- OSRM 기반 실도로 경로/거리/차량 애니메이션, 네이버 지도 핀·팝업·미니맵 패턴은
  다른 여행 코스에도 그대로 재사용 가능한 형태로 작성했다.
- 장소 데이터 스키마(`PLACES` 객체: 주소/좌표/운영시간/요금/전화/사진/설명)는
  육진대 맵 장소 DB(Notion)의 필드 구조와 최대한 맞춰뒀다 — 나중에 정적 하드코딩
  대신 DB를 직접 읽어오는 구조로 바꿀 때 매핑이 수월하도록.
- 로컬 저장(localStorage)은 이 페이지엔 필요 없었지만, 플래너 앱에서 "내 코스 저장",
  "다녀온 곳 체크" 같은 기능이 생기면 그때 도입하면 된다.

---

*원주여행 소개 페이지 작업은 여기서 마무리. 육진대 여행플래너로 확장하는 논의는
다음 세션에서 이어간다.*
